# 🚦 Traffic Sign Detector

Система для автоматического распознавания дорожных знаков на изображениях.

## 🎯 Функции
- 📤 Загрузка изображений через веб-интерфейс
- ⚡ REST API для интеграций
- 🔄 Фоновая обработка через Celery
- 📊 История распознаваний в базе данных
- 🐳 Полная контейнеризация

## 🚀 Быстрый старт

### Установка
```bash
# Клонировать репозиторий
git clone https://github.com/MaryGrays/traffic-sign-detector.git
cd traffic-sign-detector

# Запустить все сервисы
docker-compose up -d

# Применить миграции
docker-compose exec web python manage.py migrate

# 4. Создайте суперпользователя (опционально)
docker-compose exec web python manage.py createsuperuser
```

### Доступ к приложению
После запуска откройте в браузере:
- 🌐 Веб-интерфейс: http://localhost:8000   Главная страница
- 📚 API документация: http://localhost:8000/api/docs/  FastAPI документация
- 🤖 ML API: http://localhost:8001/docs
- ⚙️ Админ-панель: http://localhost:8000/admin/  Управление данными

## Технологии
- Backend: Django 4.2, FastAPI
- База данных: PostgreSQL
- Очереди: Celery + Redis
- Контейнеризация: Docker Compose
- CI/CD: GitHub Actions

## 📁 Структура проекта

**Основные папки:**
- `web/` - Django приложение (веб-интерфейс)
  - `traffic_signs/` - основное приложение
  - `manage.py` - Django CLI
  - `requirements.txt` - зависимости
- `api/` - FastAPI микросервис (REST API)
- `.github/workflows/` - CI/CD пайплайны
- `docker-compose.yml` - конфигурация Docker

**Файлы:**
- `.gitignore` - игнорируемые файлы
- `README.md` - документация


## 🔌 Примеры API запросов

### Python
```python
import requests
response = requests.post("http://localhost:8001/api/detect/",
    json={"image_url": "https://example.com/sign.jpg"})
print(response.json())


